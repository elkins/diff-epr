name: Tests

on:
  workflow_call:
  push:
    branches: ["master", "main"]
  pull_request:
    branches: ["master", "main"]


permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: "3.12"
    - name: Install dependencies
      run: |
        python -m pip install pre-commit
    - name: Run pre-commit
      run: pre-commit run --all-files

  test:
    needs: lint
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11", "3.12", "3.13", "3.14"]
        numpy-version: ["1.26.4", "2.2.6", "2.3.5"]
        exclude:
        - python-version: "3.11"
          numpy-version: "2.2.6"
        - python-version: "3.11"
          numpy-version: "2.3.5"
        - python-version: "3.12"
          numpy-version: "2.2.6"
        - python-version: "3.12"
          numpy-version: "2.3.5"
        - python-version: "3.13"
          numpy-version: "1.26.4"
        - python-version: "3.14"
          numpy-version: "1.26.4"

        - python-version: '3.13'
          jax-version: "0.4.20"
        - python-version: '3.14'
          jax-version: "0.4.20"
        - os: windows-latest
          python-version: '3.14'
        - jax-version: "0.4.20"
          numpy-version: "2.2.6"
        - jax-version: "0.4.20"
          numpy-version: "2.3.5"
        jax-version:
        - 0.4.20
        - latest
    steps:
    - uses: actions/checkout@v7

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v7
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ "${{ matrix.jax-version }}" != "latest" ]; then
          pip install -e ".[dev]" "numpy==${{ matrix.numpy-version }}" "jax==${{ matrix.jax-version }}" "jaxlib==${{ matrix.jax-version }}"
        else
          pip install -e ".[dev]" "numpy==${{ matrix.numpy-version }}"
        fi
      shell: bash




    - name: Run tests
      run: |
        pytest -v --cov=diff_epr --cov-report=xml
    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v7
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
